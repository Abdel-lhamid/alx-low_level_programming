#include "main.h"

/**
 * _abs - function that returnes the absolute value
 * @n: the number we want to test
 * Return: absolute value
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
